.sass-cache
.jekyll-metadata
Gemfile.lock
*.DS_Store
.Rproj.user/
.Rhistory
.RData
.Ruserdata

/.quarto/
_freeze/
_site/

**/*.quarto_ipynb
.Renviron
!/.Renviron       # but DO track our project-level one — it only sets RENV_CONFIG_AUTOLOADER_ENABLED
.Rhistory

# Moodle backup files used to seed teaching/ — too large for git
*.mbz

# Rendered site. Built and published by GitHub Actions (see
# .github/workflows/render-cv.yml) — never committed. Pages is configured
# with Source = "GitHub Actions", not "deploy from branch /docs".
/docs/
